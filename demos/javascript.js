'use strict';

// Kek

/* kek */

/**
 * foo
 * 
 * @param {number} n kek
 */
export const kek = (n) => 2;

class Foo {
    constructor(bar) {
        this.baz = bar;
        const kek = [
            true,
            false,
            null,
            undefined,
        ]
    }

    blel(ja) {
        return ja;
    }
}

const one = 'lelele';
let two = "kekeke";
var three = `fofofo${one}`;
let b = new Foo();
