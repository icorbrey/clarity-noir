'use strict';

// Kek

/* kek */

/**
 * foo
 * 
 * @param {number} n kek
 */
export const kek = (n, m) => n * m * 2;

const match = /kek(?<foo>f|(?:o+*))[abc]/i;

class Foo {
    constructor(bar, kek) {
        this.baz = bar;
        const kek = [
            true,
            false,
            null,
            undefined,
        ]
    }

    blel(ja, el) {
        return ja;
    }
}

const one = 'lelele';
let two = "kekeke";
var three = `fofofo${one}`;
let b = new Foo();
let x = Infinity;
b.blel();
